table
  thead
    tr
      th Name
      th Last Seen
      th Location
  tbody
    - user_locations.each do |user, location|
      tr
        td = user.name
        td #{time_ago_in_words(location.created_at)} ago
        td = location.station.location